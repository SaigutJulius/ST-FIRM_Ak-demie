# Local Claude Code settings / memory / plans
.claude/

# Pre-refactor single-file backup (2.6 MB, superseded by current index.html)
index.backup.html

# Temp screenshots / scratch
_*.png
*.tmp

# OS cruft
Thumbs.db
.DS_Store

# Admin backend secrets + runtime data (NEVER commit these)
admin-backend/.env
admin-backend/data/
admin-backend/__pycache__/
admin-backend/.venv/
*.pyc
